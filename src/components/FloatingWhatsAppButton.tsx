
import React from 'react';

// This component is intentionally empty to replace the old WhatsApp button.
// It returns null to ensure nothing renders on the screen.
const FloatingWhatsAppButton = () => {
  return null;
};

export default FloatingWhatsAppButton;
